---
import DocsLayout from '../layouts/DocsLayout.astro';
import ByTopicCatalog from '../components/ByTopicCatalog.astro';
---

<DocsLayout
  title="Topics"
  description="Curated inline examples grouped by topic with expandable source."
>
  <p>This view keeps examples in one page and adds context for each file.</p>
  <p>Use it when you need direct comparison across related examples.</p>

  <ByTopicCatalog />
</DocsLayout>
