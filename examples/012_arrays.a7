// Arrays in A7

io :: import "std/io"

main :: fn() {
    io.println("=== Arrays ===")

    numbers: [5]i32 = [10, 20, 30, 40, 50]

    sum := 0
    for i, value in numbers {
        io.println("numbers[{}] = {}", i, value)
        sum += value
    }

    io.println("sum = {}", sum)
}
