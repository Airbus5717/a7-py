// Pointer syntax in A7

io :: import "std/io"

increment :: fn(p: ref i32) {
    p.val += 1
}

main :: fn() {
    io.println("=== Pointers ===")

    x: i32
    x = 10
    p: ref i32 = x.adr

    io.println("before = {}", x)
    increment(p)
    io.println("after = {}", x)
}
