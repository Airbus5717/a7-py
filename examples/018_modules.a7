// Module imports in A7

io :: import "std/io"
math :: import "std/math"

main :: fn() {
    io.println("=== Modules ===")

    x := 9.0
    io.println("sqrt({}) = {}", x, math.sqrt(x))
    io.println("abs(-5) = {}", math.abs(-5.0))
}
